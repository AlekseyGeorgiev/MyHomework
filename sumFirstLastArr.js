function sumFirstLastArrElement(arr) {
        // let first = arr[0];
        // let last = arr[arr.length - 1];

       console.log(arr[0] + arr[arr.length - 1]);
        // console.log(first + last);
}
// sumFirstLastArrElement([20, 30, 40]);
sumFirstLastArrElement([10, 17, 22, 33]);
// sumFirstLastArrElement([11, 58, 69]);
// sumFirstLastArrElement([5]);